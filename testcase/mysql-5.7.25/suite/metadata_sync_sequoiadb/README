一、CI 对接元数据同步工具的测试用例，准备环境如下：
1. 同一台机器准备两个 MySQL 实例，如主机名A、B，端口号分别为 p1、p2

2. 启动 HA 同步组，使实例之间的元数据可互相同步，配置步骤如下：
1）在实例A、实例B的配置文件处的 [mysqld] 下配置如下参数：
server_ha_inst_group_name=xxx                  (名称自定义，可以取CI工程命名)
server_ha_inst_group_key=yyy                   (key自定义，可与上面同名那个)
core-file                                      (core的时候能生成core文件)
2）切到 MySQL 的安装目录，执行一下命令（当 MySQL 跟 SequoiaDB 在同一台机器上时，不需要指定 --host）
bin/ha_inst_group_init xxx --verbose --key yyy --host=localhost:11810
3）重启实例A、实例B
4）执行步骤 3）以后，可以看到俩实例的数据目录下，生成 myid 这个文件
5）执行步骤 3）以后，登录 sdb shell，可以看到多出了 HA 开头的 cs
6）在实例A中创建普通用户如sdbadmin，并且授予最大权限，该用户将自动同步至实例B
mysql> CREATE USER 'sdbadmin'@'%' IDENTIFIED BY 'sdbadmin';
mysql> GRANT all on *.* TO 'sdbadmin'@'%' with grant option;

二、CI 切到 /opt/sequoiasql/mysql/mysql-test，执行一下命令：
./mysql-test-run.pl --suite=metadata_sync_sequoiadb --meta-sync-addr=A:p1,B:p2 --meta-sync-user=sdbadmin --meta-sync-passwd=sdbadmin --retry=1 --retry-failure=1 --force --parallel=2
